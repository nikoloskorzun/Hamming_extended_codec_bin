cmake_minimum_required(VERSION 3.8) # Проверка версии CMake.
									# Если версия установленой программы
									# старее указаной, произайдёт аварийный выход.

project("LAB1")				# Название проекта
add_subdirectory("hamming_codec_lab")
